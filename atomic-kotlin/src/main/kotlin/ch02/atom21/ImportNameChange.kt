package ch02.atom21

import kotlin.math.PI as circleRatio
import kotlin.math.cos as consine

fun main() {
    println(circleRatio)
    println(consine(circleRatio))
    println(consine(2 * circleRatio))
}